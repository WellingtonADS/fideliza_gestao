pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Required for react-native-vision-camera
        maven { url 'https://www.jitpack.io' }
    }
}

rootProject.name = 'fideliza_gestao'
include ':app'
