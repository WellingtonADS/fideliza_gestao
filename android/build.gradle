// Top-level build file for the root project.

// In modern React Native versions, this file uses the 'plugins' block
// instead of the older 'apply plugin:' syntax.
plugins {
    // This is the correct way to apply the React Native root project plugin,
    // which resolves the "Plugin not found" error.
    id("com.facebook.react.rootproject")
}
